/// Campos del formulario de alta de conejo enlazables por voz.
enum VoiceFormField {
  name,
  breed,
  sex,
  birthDate,
  weight,
  status,
  notes,
}

/// Modo de asistencia por voz sobre un formulario (sin wizard conversacional).
enum VoiceFormMode {
  inactive,
  active,
}
